machine:
  node:
    version: 5.9.0

dependencies:
  override:
    - npm install

deployment:
  production:
    branch: master
    commands:
      - ./node_modules/.bin/bower install
      - ./node_modules/.bin/gulp
      - git clone ${DEPLOY_TARGET_REPOSITORY} dist
      - ./deploy.sh
